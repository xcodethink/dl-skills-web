> 来源：[everything-claude-code](https://github.com/affaan-m/everything-claude-code) by Affaan Mustafa

# 检查点管理（Checkpoint Management）

## 概述

检查点管理是一种在开发工作流中创建、验证和追踪进度快照的纪律性技能。通过 `/checkpoint` 命令，在关键节点创建带名称的检查点，后续可对比验证开发进展（文件变更、测试通过率、覆盖率等），确保复杂功能开发过程可控、可回溯。

---

## 用法

```
/checkpoint [create|verify|list] [name]
```

## 创建检查点（Create）

创建检查点时执行以下步骤：

1. 运行 `/verify quick` 确保当前状态是干净的
2. 创建 git stash 或带检查点名称的 commit
3. 将检查点记录到 `.claude/checkpoints.log`：

```bash
# 记录格式：日期时间 | 检查点名称 | Git 短哈希
echo "$(date +%Y-%m-%d-%H:%M) | $CHECKPOINT_NAME | $(git rev-parse --short HEAD)" >> .claude/checkpoints.log
```

4. 报告检查点已创建

## 验证检查点（Verify）

对比验证当前状态与某个检查点时：

1. 从日志中读取检查点信息
2. 将当前状态与检查点进行对比：
   - 检查点之后新增的文件
   - 检查点之后修改的文件
   - 当前与当时的测试通过率
   - 当前与当时的覆盖率

3. 输出报告：

```
检查点对比：$NAME
============================
变更文件数：X
测试：+Y 通过 / -Z 失败
覆盖率：+X% / -Y%
构建：[通过/失败]
```

## 列出检查点（List）

显示所有检查点及其：

- 名称
- 时间戳
- Git SHA（提交哈希）
- 状态（当前 / 落后 / 领先）

## 清除检查点（Clear）

移除旧的检查点（保留最近 5 个）。

---

## 典型工作流

```
[开始] --> /checkpoint create "feature-start"
   |
[实现] --> /checkpoint create "core-done"
   |
[测试] --> /checkpoint verify "core-done"
   |
[重构] --> /checkpoint create "refactor-done"
   |
[提交 PR] --> /checkpoint verify "feature-start"
```

### 工作流说明

1. **功能开发开始时** — 创建起始检查点 `feature-start`，记录干净的初始状态
2. **核心功能完成时** — 创建中间检查点 `core-done`，标记里程碑
3. **测试阶段** — 验证 `core-done` 检查点，确认核心功能实现后的测试状态
4. **重构完成后** — 创建检查点 `refactor-done`，确保重构没有引入回归
5. **准备提交 PR 时** — 验证 `feature-start` 检查点，全面对比整个功能开发周期的变更

---

## 命令参数

| 参数 | 说明 |
|------|------|
| `create <name>` | 创建命名检查点 |
| `verify <name>` | 对比验证指定检查点 |
| `list` | 显示所有检查点 |
| `clear` | 移除旧检查点（保留最近 5 个） |

---

## 最佳实践

1. **在每个重要里程碑创建检查点** — 而非在每个小改动后创建
2. **使用有意义的检查点名称** — 如 `auth-backend-done`、`db-migration-applied`
3. **在重构前后各创建检查点** — 方便验证重构未引入回归
4. **提交 PR 前验证初始检查点** — 全面审视整个功能的变更范围
5. **定期清理旧检查点** — 使用 `clear` 保持日志整洁
